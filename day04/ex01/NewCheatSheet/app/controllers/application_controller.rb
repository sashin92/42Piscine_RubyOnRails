# In general, if you have a feature that's definitely available in all other controllers,
# then ApplicationController might be a good fit for you.
class ApplicationController < ActionController::Base
end
